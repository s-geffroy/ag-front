{
  "name": "@ag/lead-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public self-hosted lead endpoint (POST /api/lead) behind Caddy. Feeds the cockpit acquisition pipeline.",
  "scripts": {
    "start": "tsx server/index.ts",
    "dev": "tsx watch server/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ag/schema": "*",
    "express": "^4.21.1",
    "nodemailer": "^6.9.16",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.9.0",
    "@types/nodemailer": "^6.4.16",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
