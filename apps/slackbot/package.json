{
  "name": "@ag/slackbot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Socket Mode Slack listener: promote a news cluster from a private channel (ADR 0071/0074, P2).",
  "scripts": {
    "start": "tsx server/index.ts",
    "dev": "tsx watch server/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "stream": "tsx server/stream-run.ts",
    "pulse": "tsx server/pulse-run.ts"
  },
  "dependencies": {
    "@ag/chokepoints": "*",
    "@slack/bolt": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
